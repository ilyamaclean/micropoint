% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/NewMicroPoint.R
\name{weatherhgt_adjust}
\alias{weatherhgt_adjust}
\title{Convert standard meteorological observations to above-canopy forcing}
\usage{
weatherhgt_adjust(
  climdata,
  zin,
  zout,
  lat,
  long,
  SoilTempIni = NA,
  ThetaIni = NA,
  CO2ppm = NA,
  boundaryT = NA
)
}
\arguments{
\item{climdata}{data.frame of weather data matching the format of the inbuilt
dataset `climdata`}

\item{zin}{Numeric. Height (m) at which the meteorological variables
were measured.}

\item{zout}{Numeric. Height (m) used as meteorological forcing.}

\item{lat}{Numeric. Latitude in decimal degrees.}

\item{long}{Numeric. Longitude in decimal degrees.}

\item{SoilTempIni}{Optional numeric vector of initial soil temperatures.
If `NA`, a default profile is generated.}

\item{ThetaIni}{Optional numeric vector of initial volumetric soil water
contents. If `NA`, all layers are initialised to 0.419.}

\item{CO2ppm}{Optional numeric. Atmospheric CO₂ concentration (ppm).
If `NA`, it is estimated from the year of the first observation using [Cafromyear()].}

\item{boundaryT}{Optional numeric. Approximate mean annual air temperature
(°C) used to initialise the soil temperature profile. If `NA`, it is
estimated from `climdata$temp`.}
}
\value{
A data.frame identical to `climdata` but with `temp`, `relhum`,
`windspeed`, and `pres` converted to height `zout`.
}
\description{
Converts meteorological observations measured at height `zin`
(typically ~2 m above ground) to height `zout` so they can be used as
meteorological forcing when vegetation height exceeds the measurement
height and the observations therefore lie within the canopy rather than
above it.
}
\details{
Standard meteorological observations are typically made about 2 m above the
ground. If vegetation height exceeds the measurement height, these observations
lie within the canopy. The microclimate model requires meteorological forcing
above the canopy, so the observations are adjusted from height `zin` to height
`zout` before being used to force the model.
}
