% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/NewMicroPoint.R
\name{InitailizeWater}
\alias{InitailizeWater}
\title{Runs model quickly for a period of time to get an initial soil water profile}
\usage{
InitailizeWater(
  climdata,
  vegp,
  soilc,
  lat,
  long,
  zref = 2,
  zmr = 0.004,
  CO2ppm = NA,
  boundaryT = NA,
  maxiter = 100,
  C3 = TRUE,
  Lfrac = NA
)
}
\arguments{
\item{climdata}{data.frame of weather conditions that uses the same naming conventions
as in the inbuilt dataset `climdata`.}

\item{vegp}{Vegetation parameter list as returned by [createvegp()].
If `NA`, bare-ground conditions are assumed, as are canopies shorter than
1 cm, which are too short for the vegetation to present the wind a surface
distinct from the soil.}

\item{soilc}{Soil parameter list as returned by [createsoilc()].}

\item{lat}{Numeric. Latitude in decimal degrees.}

\item{long}{Numeric. Longitude in decimal degrees.}

\item{zref}{Numeric. Height (m) of the meteorological forcing data.
Default is 2 (see Details).}

\item{zmr}{Numeric. Roughness length for bare-ground calculations.}

\item{CO2ppm}{Optional numeric. Atmospheric CO2 concentration (ppm).
If `NA`, it is estimated from the year of the first observation using [Cafromyear()].}

\item{boundaryT}{Optional numeric. Approximate mean annual air temperature
used to initialise the soil temperature profile. If `NA`, it is estimated
from `climdata$temp`, which usually requires roughly a full year of data.}

\item{maxiter}{Integer. Maximum number of iterations used to achieve convergence
within each time step.}

\item{C3}{Logical. If `TRUE`, use C3 photosynthesis; otherwise C4.}

\item{Lfrac}{Fraction of plant area that is living vegetation, given either as
a single value or as a vector giving the fraction in each canopy layer.
Required unless `vegp` is `NA`.}
}
\value{
Numeric vector of initial volumetric soil water contents in each layer
}
\description{
Runs model quickly for a period of time to get an initial soil water profile
}
\details{
The soil water model is run over the supplied climate data, and the final
soil water profile is used as the initial condition. Where antecedent climate
data are not available, use data from the corresponding period in the current
year ending immediately before the time of interest.
}
